\begin{gather*}
    \begin{split}
        (\lambda z. (\lambda x . yx) xz) [x:=zx] & \cong (\lambda a. (\lambda b . yb) xa) [x:=zx] \\
        & = (\lambda a. (\lambda b . yb) zxa) \\
        & = (\lambda a. yzxa)
    \end{split}
\end{gather*}